name: Gemini feasibility pilot (completed)

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: Type REOPEN_PILOT only if a new feasibility attempt is intentionally required
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: gemini-feasibility-pilot
  cancel-in-progress: false

jobs:
  pilot:
    if: inputs.confirmation == 'REOPEN_PILOT'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[api,dev]"
      - name: Validate existing Version 2 design
        run: make v2-validate
      - name: Run non-confirmatory Gemini pilot
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: gemini-3.6-flash
        run: bash scripts/run_gemini_pilot.sh
      - name: Preserve pilot attempt and Claude design lock
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gemini-pilot-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 90
          path: |
            outputs/gemini_pilot/
            docs/claude_confirmatory_design_lock.json

name: Claude confirmatory live audit

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: Type RUN_CLAUDE to confirm this is the prospectively locked Claude run
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: claude-confirmatory-audit
  cancel-in-progress: false

jobs:
  audit:
    if: inputs.confirmation == 'RUN_CLAUDE'
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      MPLCONFIGDIR: /tmp/matplotlib
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[api,dev]"
      - name: Validate existing Version 2 design
        run: make v2-validate
      - name: Run prospectively code-locked Claude confirmatory program
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_MODEL: claude-sonnet-4-6
        run: bash scripts/run_claude_confirmatory.sh
      - name: Preserve every attempted result and design lock
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: claude-confirmatory-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 90
          path: |
            outputs/claude_confirmatory/
            docs/claude_confirmatory_design_lock.json
